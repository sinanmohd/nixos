monitoring:
  redis:
    endpoint: "valkey.valkey:6379"
    password: "valkey"
  postgres:
    endpoint: "postgres-r.postgres:5432"
    username: "monitoring"
    password: "monitoring"

opentelemetry-operator:
  enabled: true
