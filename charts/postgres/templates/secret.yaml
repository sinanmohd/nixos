{{- range $.Values.users }}
apiVersion: v1
kind: Secret
type: kubernetes.io/basic-auth
metadata:
  name: "{{ .name }}-cnpg-user"
  labels:
    cnpg.io/reload: "true"
data:
  username: {{ .name | b64enc | quote }}
  password: {{ .name | b64enc | quote }}
---
{{- end -}}
